version: 2
project_name: teamcode
before:
  hooks:
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/ETEllis/teamcode/internal/version.Version={{.Version}}
    main: ./main.go

archives:
  - formats: ["tar.gz"]
    name_template: >-
      teamcode-
      {{- if eq .Os "darwin" }}mac-
      {{- else if eq .Os "windows" }}windows-
      {{- else if eq .Os "linux" }}linux-{{end}}
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "#86" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: ["zip"]
checksum:
  name_template: "checksums.txt"
snapshot:
  version_template: "0.0.0-{{ .Timestamp }}"
aurs:
  - name: teamcode-bin
    homepage: "https://github.com/ETEllis/teamcode"
    description: "terminal based agent that can build anything"
    maintainers:
      - "kujtimiihoxha <kujtimii.h@gmail.com>"
    license: "MIT"
    private_key: "{{ .Env.AUR_KEY }}"
    git_url: "ssh://aur@aur.archlinux.org/teamcode-bin.git"
    provides:
      - teamcode
    conflicts:
      - teamcode
    package: |-
      install -Dm755 ./teamcode "${pkgdir}/usr/bin/teamcode"
homebrew_casks:
  - repository:
      owner: ETEllis
      name: homebrew-tap
    binaries:
      - teamcode
    homepage: "https://github.com/ETEllis/teamcode"
    description: "Terminal-based AI coding team assistant"
nfpms:
  - maintainer: kujtimiihoxha
    description: terminal based agent that can build anything
    formats:
      - deb
      - rpm
    file_name_template: >-
      {{ .ProjectName }}-
      {{- if eq .Os "darwin" }}mac
      {{- else }}{{ .Os }}{{ end }}-{{ .Arch }}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^doc:"
      - "^test:"
      - "^ci:"
      - "^ignore:"
      - "^example:"
      - "^wip:"
